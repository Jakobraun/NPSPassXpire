# Email templates

Outlook `.oft` files are maintained locally and excluded from Git because they may contain organization-specific wording, sender details, or embedded account metadata.

Before adding a template to the repository, inspect its content and metadata, remove personal or confidential information, and confirm that the organization permits publication.

The current application does not load these files programmatically. They are optional templates for manually sending the email groups displayed by the report.
